# Working Notes

- Fixed streaming duplication / raw XML leakage across `chat.completions`, `responses`, and Claude Code `/v1/messages`.
- Added regression tests for:
  - OpenAI chat streaming with tools
  - OpenAI responses streaming with tools
  - Claude Code streaming text around XML tool calls
- Updated launcher `.bat` files at repo root and under `claude-proxy/` to reflect the new text/tool stream guard and UTF-8 console setup.
- Verified with `python -m pytest -q` from `claude-proxy/`: `38 passed, 5 subtests passed`.
- Verified `python -m compileall C:\Users\qiyan\Desktop\flowith-claude-proxy\claude-proxy\proxy -q`.
- Follow-up fix: `responses` streaming with tools no longer repeats completed text in final SSE payloads.
  - Root cause: some clients render `response.output_text.delta`, `response.output_text.done`, `response.output_item.done`, and `response.completed` text fields together.
  - Fix: in tools streaming mode, visible text is emitted through deltas only; final structural events keep empty text fields.
  - Manual verification showed `delta` chunks once each and `full_text_field=0`, `full_output_text=0`.
- Existing dirty files left untouched for now:
  - `claude-proxy/proxy/__main__.py`
  - `claude-proxy/proxy/upstream.py`
- Those files appear to be prior tracing / UTF-8 / debug-instrumentation changes, not part of the streaming fix.
- 2026-06-28: Fixed startup ImportError by adding `FLOWITH_TRACE_HERMES` to `claude-proxy/proxy/config.py`; added `claude-proxy/tests/test_config.py`. Verified with `python -m pytest tests -q` and a temporary uvicorn `/health` check on port 8799.
- 2026-06-28: Fixed launcher hang on `[INFO] Another launcher is installing dependencies. Waiting...`; root cause was stale empty `claude-proxy/.install.lock` with no active installer process. Updated all root and `claude-proxy/` `start*.bat` files to clear locks older than 300s when no `venv/pip/ensurepip` process is active. Verified stale-lock reproduction before the fix, stale-lock cleanup through `start-codex.bat`, no remaining `.install.lock`, no proxy process/ports 8787-8789 left from verification, and `python -m pytest tests -q` from `claude-proxy/` (`39 passed, 1 warning, 5 subtests passed`).
- 2026-06-28: Added root `clean.bat` for manual cleanup. Default cleanup removes an empty `claude-proxy/.install.lock`, root/proxy pytest caches, and project bytecode caches; `.env`, logs, and `claude-proxy/venv` are kept unless `--venv` is passed. README documents `clean.bat` and `clean.bat --venv`. Verified by creating an empty lock and `proxy/__pycache__/codex_clean_probe.tmp`, running `cmd /c clean.bat --no-pause`, confirming `lock_exists=False`, `probe_exists=False`, `venv_exists=True`, plus `cmd /c clean.bat --help` and `git diff --check`.
- 2026-07-02: Fixed Fable/Codex empty replies when `/v1/responses` streams with tools. Root causes: XML tool prompt over-emphasized mandatory tool use, causing `claude-fable-5` to return empty for direct-answer requests with tools; Responses streaming also blanked final `output_text.done` / `response.completed.output_text` whenever tools were present, so Codex could overwrite visible deltas with empty text. Changed `claude-proxy/proxy/adapter.py` prompt to allow direct answers when no tool is needed; changed `claude-proxy/proxy/codex/router.py` to blank final text only when function calls were actually emitted. Added regressions in `claude-proxy/tests/test_tool_bridge.py` and `claude-proxy/tests/test_openai_proxy.py`. Verified with focused tests, `python -m pytest tests -q` three rounds (`51 passed, 5 subtests passed` each), `python -m compileall proxy -q`, `git diff --check`, and live `claude-fable-5` + tools probes for both plain text (`fable-tools-ok` in delta/done/completed) and shell tool call (`shell` function_call, no raw XML leak).
- 2026-07-03: Matured install/security/probe defaults against UniClaudeProxy reference: added `FLOWITH_LOCAL_ONLY`, localhost-only middleware, `.env.example`, Docker/Compose `FLOWITH_*` config, `/models` alias, `HEAD /`, cleaned `start.sh`, and ignored debug/reference artifacts. Verified from `claude-proxy/` with `python -m pytest -q` (`54 passed, 6 subtests passed`), `python -m compileall proxy -q`, `git diff --check`, secret-pattern scan (only placeholders/test keys), and temporary port 8799 probes (`HEAD /` 200, `/health` ok, `/models` 10). Docker CLI was not installed locally, so Compose was not executable-verified.
- 2026-07-03: Fixed reverse-proxy streaming chunk truncation when the final visible chunk ends with a partial think-tag prefix such as `<th`. Root cause: pending think-prefix buffers in `claude-proxy/proxy/codex/router.py` and `claude-proxy/proxy/server.py` were held to await another chunk, then never flushed at stream end. Added regressions for `/v1/chat/completions`, `/v1/responses`, and Claude `/v1/messages`; fix flushes pending visible think-prefix tails only when not inside a confirmed think block. Verified from `claude-proxy/` with focused tests, `python -m pytest -q` (`57 passed, 6 subtests passed`), `python -m compileall proxy -q`, and `git diff --check`. Added reusable project skill `.codex/skills/llm-proxy-streaming/SKILL.md`; validated with skill-creator `quick_validate.py` (`Skill is valid!`).
- 2026-07-03: Fixed Claude Code `/goal` prompt Stop hook failures through the Anthropic reverse proxy. Root cause: `/goal` is a prompt-type Stop hook that expects strict JSON `{ok, reason}`, but upstream returned a natural-language Chinese summary, causing Claude Code `JSON validation failed`. Added hook/goal JSON request detection and non-streaming result normalization in `claude-proxy/proxy/server.py`; added regression `test_goal_stop_hook_plain_text_response_becomes_strict_json` in `claude-proxy/tests/test_tool_bridge.py`. Also updated user-level `legacy user Stop hook shim` to emit UTF-8 `hookSpecificOutput.additionalContext` instead of `decision:block`, avoiding garbled Stop hook feedback/error styling. Verified with focused pytest, full `python -m pytest -q` (`58 passed, 1 warning, 6 subtests passed`), `python -m compileall proxy -q`, `git diff --check` (line-ending warnings only), and manual JSON parse of the updated hook output shape.
- 2026-07-04: Fixed the remaining Claude Code `/goal` Stop hook `JSON validation failed` on the streaming Anthropic `/v1/messages` path. Root cause: `/goal` sends `stream=true`; only the non-streaming hook JSON path had been normalized, so streaming SSE still forwarded natural-language text. Added `_stream_hook_json_events` in `claude-proxy/proxy/server.py` to call upstream non-streaming for hook JSON checks and emit strict JSON through Anthropic SSE; added regression `test_streaming_goal_stop_hook_plain_text_response_becomes_strict_json`. Verified from `claude-proxy/` with focused pytest (`2 passed, 1 warning`), `python -m compileall proxy -q`, full `python -m pytest -q` (`59 passed, 1 warning, 6 subtests passed`), restarted the local proxy on port 8787 (new PID 9428), `/health` returned `{"ok":true}`, and a live streaming `/v1/messages` Stop hook probe returned parseable JSON with boolean `ok` and non-empty `reason`.
- 2026-07-04: Updated Windows launch/cleanup scripts for the current local-proxy workflow. `claude-proxy/start.bat`, `start-codex.bat`, and `start-hermes.bat` now treat a healthy existing proxy on the target port as OK and exit successfully instead of showing a port-conflict error. `clean.bat` now delegates to `scripts/clean.ps1`, skips in-use log files without PowerShell `Remove-Item` red errors, and supports opt-in `--stop-proxy` to stop local `python -m proxy` listeners on ports 8787/8788/8789 before cleanup. Verified with `cmd /c start.bat`, `cmd /c claude-proxy\start.bat`, `cmd /c clean.bat --help`, `cmd /c clean.bat --no-pause` while port 8787 was running, `clean.ps1` parse check, `/health` on 8787, and `git diff --check` (CRLF warnings only).
- 2026-07-04: Fully exercised `clean.bat --stop-proxy --no-pause`: it stopped the running 8787 proxy PID 9428, deleted `proxy_stderr.log` and `proxy_stdout.log`, and completed without errors. Restarted the Claude proxy on 8787 as PID 10532, verified `/health` returned `{"ok":true}`, then reran `cmd /c start.bat` (healthy existing proxy exits OK) and `cmd /c clean.bat --no-pause` (in-use logs are kept without red PowerShell errors).
- 2026-07-05: Re-verified the Claude Code empty/invalid-response fixes around tool XML parsing, split CDATA parameter recovery, strict Stop hook JSON normalization, and final partial think-tag flushing. Current evidence from `claude-proxy/`: `python -m pytest -q` -> 63 passed, 6 subtests passed; `python -m compileall proxy -q` OK; `git diff --check` OK; focused streaming/tool/reasoning tests -> 51 passed, 6 subtests passed. Claude Code debug logs showed plugin marketplace schema/certificate warnings but no direct skill/hook crash; current Stop hook emits additionalContext rather than blocking JSON.
- 2026-07-06: Re-verified current auth/local-only/request-limit/upstream-stability changes from claude-proxy/ and cleaned two mojibake em-dash comments in proxy/server.py. Evidence: full python -m pytest -q (87 passed, 1 warning, 51 subtests passed), python -m compileall proxy -q, git diff --check, no mojibake marker hits in relevant text files, and security scans for secret-shaped tokens / sensitive logging / command-SQL-HTML sinks. Secret scan hits were expected config names, docs, placeholders, and test keys; no sensitive logging or sink hits.
- 2026-07-06: Added product-polish hardening for config and upstream stability. Config now ignores placeholder `FLOWITH_API_KEY` values and can fall back to `.flowith_api_key`; blank `FLOWITH_DEBUG_DUMP_DIR` falls back to the project debug dump dir. Upstream debug dumps now redact secret-shaped fields, truncate large payload/response strings, and prune old dumps; HTTP error results no longer expose upstream body details; upstream concurrency uses a bounded acquire timeout. Added regressions in `claude-proxy/tests/test_config.py` and `claude-proxy/tests/test_upstream_stability.py`. Verified from `claude-proxy/` with `python -m pytest tests/test_config.py -q` (10 passed), `python -m pytest tests/test_upstream_stability.py -q` (18 passed), full `python -m pytest -q` (92 passed, 1 warning, 51 subtests passed), `python -m compileall proxy -q`, `git diff --check`, and secret/sensitive-logging scans.
- 2026-07-06: Completed follow-up security hardening audit from `_review/security.md`: Docker image now runs as non-root `proxy`; `.env.example`/`claude-proxy/README.md` document `FLOWITH_SEMAPHORE_TIMEOUT`, request body limit, debug dump truncation/pruning/redaction, unsafe network binding refusal, and Docker security defaults. Fresh verification from `claude-proxy/`: `python -m pytest -q` -> 96 passed, 1 warning, 51 subtests passed; focused current security regressions -> 10 passed, 1 warning, 42 subtests passed; `python -m compileall proxy -q` exit 0; `git diff --check` exit 0; focused secret scan only found placeholders/test/config names; sensitive logging scan had no hits; Dockerfile grep confirmed `USER proxy` and port 8787 healthcheck.
- 2026-07-06: Productized local proxy dashboard/launcher/Hermes maintenance changes before commit. Added read-only `/dashboard` plus `/dashboard/api/status|config|routes|debug-dumps`, masked config/debug-dump metadata, auto-open dashboard launchers, healthy-existing-proxy launcher exits, Hermes smoke script, project AGENTS routing, and reusable local skills for streaming/Hermes commit workflows. Cleaned one-off repro/review/reference artifacts before staging. Fresh evidence: from `claude-proxy/`, `python -m pytest -q` -> 107 passed, 1 warning, 51 subtests passed; `python -m compileall proxy -q` OK; repo `git diff --check` OK with CRLF warnings only; skill validation for github-commit-guidelines/hermes-proxy-compat/llm-proxy-streaming all OK; secret-like scan found only config symbol names (`require_discovery_api_key`), no real secrets.
